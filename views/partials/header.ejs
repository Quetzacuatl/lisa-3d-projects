<!DOCTYPE html>
<html lang="nl" class="h-full bg-stone-50 text-stone-900">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lisa. 3D Studio</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            brand: {
              peach: '#FFFaf0',
              rose: '#f43f5e',
              coral: '#f43f5e',
              amber: '#f59e0b',
            }
          },
          fontFamily: {
            sans: ['Plus Jakarta Sans', 'sans-serif'],
            serif: ['Playfair Display', 'serif'],
          }
        }
      }
    }
  </script>
  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Plus Jakarta Sans', sans-serif;
    }
    .serif-title {
      font-family: 'Playfair Display', serif;
    }
    /* Verberg scrollbars maar behoud scroll functionaliteit */
    .no-scrollbar::-webkit-scrollbar {
      display: none;
    }
    .no-scrollbar {
      -ms-overflow-style: none;
      scrollbar-width: none;
    }
  </style>
</head>
<body class="h-full flex flex-col bg-gradient-to-b from-[#fdfbf7] via-[#fff5f5] to-[#f4f2ff] text-stone-850 antialiased overflow-x-hidden selection:bg-rose-500/10 selection:text-rose-600">
  
  <!-- Header / Navigatie (Geoptimaliseerd voor mobiel) -->
  <header class="border-b border-rose-100/40 bg-white/60 backdrop-blur-xl sticky top-0 z-40">
    <div class="max-w-md mx-auto px-6 py-4 flex items-center justify-between">
      <a href="/" class="flex items-center space-x-2">
        <span class="serif-title text-2xl font-bold tracking-tight text-stone-900">Lisa<span class="text-rose-500">.</span></span>
        <span class="text-[9px] uppercase tracking-widest bg-amber-100/60 text-amber-800 px-2 py-0.5 rounded-full font-mono font-bold">3D Studio</span>
      </a>
      
      <a href="/admin" class="text-xs text-stone-600 hover:text-stone-950 font-mono tracking-widest uppercase transition bg-white/80 hover:bg-stone-50 px-3.5 py-1.5 rounded-full border border-stone-200/60 shadow-sm">
        Admin
      </a>
    </div>
  </header>

  <!-- Hoofdcontainer: Altijd mobiele breedte, gecentreerd op grote schermen -->
  <main class="flex-grow max-w-md w-full mx-auto pb-12 flex flex-col">
